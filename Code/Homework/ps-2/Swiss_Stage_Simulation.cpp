/**
 * @file Swiss_Stage_Simulation.cpp
 * @brief 瑞士轮赛制模拟 - 分组、Bu 分、首尾配对、严格调用伪随机函数
 *
 * 【题目描述】
 * 有 2^n 支队伍参加瑞士轮比赛。每支队伍初始有一个种子编号：
 * 编号越小，表示这支队伍越强。
 *
 * 每场比赛：
 *  - 胜者胜场 +1
 *  - 负者负场 +1
 *
 * 当一支队伍：
 *  - 胜场达到 k ：晋级，离开瑞士轮阶段
 *  - 负场达到 k ：淘汰，离开瑞士轮阶段
 *
 * 每一轮比赛前，需要将所有“还没离开的队伍”按 (win, lose) 分组。
 * 同一组内：
 *  - 所有队伍的胜负场完全相同
 *  - 先比较 Bu 分，Bu 分更高的更强
 *  - 若 Bu 分相同，则种子编号更小的更强
 *  - 每次取最强的一支与最弱的一支比赛，直到这一组全部配对完成
 *
 * 不同组之间：
 *  - 必须先处理“胜场更多”的组
 *  - 例如 2-1 组要先于 1-2 组完成比赛
 *
 * 【Bu 分定义】
 * 一支队伍的 Bu 分，等于它此前所有对手“当前”的 (win - lose) 之和。
 *
 * 注意：
 * 1. Bu 分在一整轮内固定。必须在这一轮开始时统一计算好。
 * 2. 如果之前多次遇到同一个对手，则每次都要重复累计。
 * 3. 如果还没打过任何比赛，则 Bu 分为 0。
 *
 * 【比赛结果如何决定】
 * 题目给出一个伪随机函数：
 *
 * static unsigned long long seed = 1;
 * unsigned int my_rand(void) {
 *     seed = seed * 1103515245 + 12345;
 *     return seed & 1;
 * }
 *
 * 每场比赛必须严格调用一次该函数：
 *  - 返回 1 ：强队赢
 *  - 返回 0 ：弱队赢
 *
 * 【输出要求】
 * 对每一个查询队伍，按比赛顺序输出：
 *  - 它遇到过的所有对手名字
 *  - 最后输出它的最终战绩 win-lose
 *
 * 例如：
 * 对手1 对手2 对手3 ... win-lose
 *
 * --------------------------------------------------
 * 【核心思路】
 *
 * 这是一道“复杂规则模拟题”，关键不是高深算法，而是把规则拆成稳定模块：
 *
 * 1. 为每支队伍维护完整状态：
 *    - 队名
 *    - 种子编号
 *    - 当前 Bu 分
 *    - 当前胜场 / 负场
 *    - 是否已经离开瑞士轮
 *    - 历史对手列表（用下标记录）
 *    - 每场结果（本题输出不直接用到，但保留下来便于完整记录）
 *
 * 2. 每一轮开始时：
 *    - 先统一计算所有在赛队伍的 Bu 分
 *    - 再按 (win, lose) 分组
 *    - 再按“胜场多的组先处理”
 *    - 每组内部按“强到弱”排序
 *    - 然后首尾配对：最强打最弱，次强打次弱……
 *
 * 3. 每一场比赛：
 *    - 题目规定返回 1 表示强队获胜，返回 0 表示弱队获胜
 *    - 因此 play_match() 的参数约定为：
 *          第一个参数一定是强队，第二个参数一定是弱队
 *    - 这样函数内部就不用再次判断强弱
 *
 * 4. 整个瑞士轮阶段：
 *    - 不断执行“单轮模拟”
 *    - 直到所有队伍都达到 k 胜或 k 负，全部离开瑞士轮
 *
 * --------------------------------------------------
 * 【为什么要存对手下标，而不是直接存 Team 对象？】
 *
 * 因为队伍状态（win / lose / bu）会随着比赛不断变化。
 * 如果把整个 Team 对象拷贝进历史记录，那么后续计算 Bu 分时，
 * 取到的会是“旧拷贝”，而不是对手当前战绩。
 *
 * 所以：
 *  - 历史对手只存“对手下标”
 *  - 需要当前状态时，统一去总队伍数组 teams 中查询
 *
 * --------------------------------------------------
 * 【时间复杂度分析（大致）】
 *
 * 设总队伍数为 m = 2^n。
 * 每轮：
 *  - 计算 Bu：遍历当前在赛队伍及其历史对手
 *  - 分组：O(m)
 *  - 各组排序：总的排序代价在本数据范围内可接受
 *
 * 数据范围 n <= 14，所以 m <= 16384。
 * 本题重点是正确模拟，而不是极限优化。
 */

#include <bits/stdc++.h>
using namespace std;

// --------------------------------------------------
// 题目规定的伪随机函数：不能改动，必须严格按比赛顺序调用
// --------------------------------------------------
static unsigned long long seed = 1;
unsigned int my_rand(void)
{
    seed = seed * 1103515245 + 12345;
    return seed & 1;
}

// --------------------------------------------------
// 队伍结构体
// --------------------------------------------------
struct Team
{
    string name;          // 队伍名称
    int seed_id = 0;      // 种子编号，越小越强
    int bu = 0;           // 当前轮固定使用的 Bu 分
    int win = 0;          // 当前胜场
    int lose = 0;         // 当前负场
    bool isleave = false; // 是否已经晋级或淘汰

    vector<int> opponents; // 历史对手下标（按比赛发生顺序记录）
    vector<int> result;    // 每场结果：1 表示自己赢，0 表示自己输
};

// --------------------------------------------------
// 计算某支队伍当前的 Bu 分
//
// 定义：此前所有对手当前的 (win - lose) 之和
// 如果之前多次遇到同一个对手，则要重复累计
// --------------------------------------------------
int compute_bu(const vector<Team> &teams, int id)
{
    int sum = 0;
    for (int opp : teams[id].opponents)
    {
        sum += teams[opp].win - teams[opp].lose;
    }
    return sum;
}

// --------------------------------------------------
// 进行一场比赛
//
// 约定：传入时 strong 一定比 weak 强
// 规则：my_rand() == 1 => 强队赢；否则弱队赢
//
// 这里要更新：
// 1. 双方历史对手
// 2. 双方胜负场
// 3. 双方每场比赛结果
// 4. 是否达到 k 胜 / k 负而离开瑞士轮
// --------------------------------------------------
void play_match(vector<Team> &teams, int strong, int weak, int k)
{
    // 双方记录彼此为历史对手
    teams[strong].opponents.push_back(weak);
    teams[weak].opponents.push_back(strong);

    unsigned int x = my_rand();

    if (x == 1)
    {
        // 强队获胜
        teams[strong].win++;
        teams[weak].lose++;
        teams[strong].result.push_back(1);
        teams[weak].result.push_back(0);
    }
    else
    {
        // 弱队获胜
        teams[weak].win++;
        teams[strong].lose++;
        teams[strong].result.push_back(0);
        teams[weak].result.push_back(1);
    }

    // 达到 k 胜或 k 负，则离开瑞士轮
    if (teams[strong].win == k || teams[strong].lose == k)
        teams[strong].isleave = true;
    if (teams[weak].win == k || teams[weak].lose == k)
        teams[weak].isleave = true;
}

// --------------------------------------------------
// 模拟一整轮比赛
//
// 一轮中的关键步骤：
// 1. 统一计算所有在赛队伍的 Bu 分（这一轮内固定）
// 2. 按 (win, lose) 分组
// 3. 按“胜场更多的组优先”处理
// 4. 每组内部按“强到弱”排序
// 5. 首尾配对并比赛
//
// 注意：
// - Bu 分不能边打一边更新，必须先统一算完
// - 每组内部排序后，直接首尾配对，不要打一场后重排
// --------------------------------------------------
void play_one_round(vector<Team> &teams, int k)
{
    // ---------- 1. 统一计算本轮 Bu 分 ----------
    for (int i = 0; i < (int)teams.size(); i++)
    {
        if (!teams[i].isleave)
        {
            teams[i].bu = compute_bu(teams, i);
        }
    }

    // ---------- 2. 按 (win, lose) 分组 ----------
    map<pair<int, int>, vector<int>> groups;
    for (int i = 0; i < (int)teams.size(); i++)
    {
        if (!teams[i].isleave)
        {
            groups[{teams[i].win, teams[i].lose}].push_back(i);
        }
    }

    // ---------- 3. 决定各组的处理顺序 ----------
    // 题目要求：胜场更多的组先处理
    vector<pair<int, int>> order;
    for (auto &it : groups)
    {
        order.push_back(it.first);
    }

    sort(order.begin(), order.end(), [](const pair<int, int> &a, const pair<int, int> &b)
         {
             if (a.first != b.first)
                 return a.first > b.first; // 胜场高的先
             return a.second < b.second;   // 若需要，再让负场少的在前
         });

    // ---------- 4. 逐组处理 ----------
    for (auto key : order)
    {
        vector<int> vec = groups[key];

        // 组内按“强到弱”排序：
        // 先比较 Bu 分（高者强），Bu 相同则种子编号小者强
        sort(vec.begin(), vec.end(), [&](int a, int b)
             {
                 if (teams[a].bu != teams[b].bu)
                     return teams[a].bu > teams[b].bu;
                 return teams[a].seed_id < teams[b].seed_id; });

        // ---------- 5. 首尾配对 ----------
        // 最强 vs 最弱，次强 vs 次弱……
        int m = (int)vec.size();
        for (int j = 0; j < m / 2; j++)
        {
            int strong = vec[j];
            int weak = vec[m - 1 - j];
            play_match(teams, strong, weak, k);
        }
    }
}

// --------------------------------------------------
// 判断是否还有队伍仍在瑞士轮中
// --------------------------------------------------
bool has_active_team(const vector<Team> &teams)
{
    for (const auto &t : teams)
    {
        if (!t.isleave)
            return true;
    }
    return false;
}

// --------------------------------------------------
// 输出某支队伍的比赛历史
//
// 题目真正要求输出的是：
// - 这支队伍按顺序遇到过的所有对手
// - 最终战绩 win-lose
//
// 不输出：
// - 自己名字
// - 每场比赛的赢家
// --------------------------------------------------
void print_team(const vector<Team> &teams, int id)
{
    for (int i = 0; i < (int)teams[id].opponents.size(); i++)
    {
        if (i)
            cout << ' ';
        cout << teams[teams[id].opponents[i]].name;
    }
    if (!teams[id].opponents.empty())
        cout << ' ';
    cout << teams[id].win << '-' << teams[id].lose << '\n';
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // --------------------------------------------------
    // 输入：
    // n, k, q
    // 共有 2^n 支队伍
    // 接着读入 2^n 个队伍名字，按输入顺序给予种子编号 1..2^n
    // 再读入 q 个查询队名
    // --------------------------------------------------
    int n, k, q;
    cin >> n >> k >> q;

    int m = 1 << n; // 队伍总数
    vector<Team> teams(m);
    unordered_map<string, int> id_of; // 队名 -> 下标
    id_of.reserve(m * 2);

    for (int i = 0; i < m; i++)
    {
        cin >> teams[i].name;
        teams[i].seed_id = i + 1; // 输入顺序就是种子编号
        id_of[teams[i].name] = i;
    }

    vector<string> queries(q);
    for (int i = 0; i < q; i++)
    {
        cin >> queries[i];
    }

    // --------------------------------------------------
    // 主循环：不断进行瑞士轮，直到所有队伍都离开
    // --------------------------------------------------
    while (has_active_team(teams))
    {
        play_one_round(teams, k);
    }

    // --------------------------------------------------
    // 按查询顺序输出结果
    // --------------------------------------------------
    for (int i = 0; i < q; i++)
    {
        print_team(teams, id_of[queries[i]]);
    }

    return 0;
}

/**
 * 【本题最关键的实现注意事项】
 *
 * 1. Bu 分是一轮一算，不是打一场变一次
 *    正确做法：
 *      - 一轮开始时统一计算所有在赛队伍的 Bu
 *      - 这一轮中分组、排序、比赛都使用这个固定值
 *
 * 2. 不能把 Team 对象直接存进历史对手列表
 *    否则以后 win / lose 变化后，历史里存的是旧拷贝
 *    正确做法：
 *      - 只存对手下标
 *      - 要看当前状态时，回到总数组 teams 中查询
 *
 * 3. play_match() 中不要重复比较强弱
 *    因为外层已经先排序，再首尾配对：
 *      - vec[0] 一定最强
 *      - vec.back() 一定最弱
 *    所以只要保证参数传入顺序不乱即可
 *
 * 4. my_rand() 一场比赛只能调用一次
 *    而且必须严格按照题目规定的比赛发生顺序调用
 *    一旦调用顺序错了，后面的所有结果都会连锁出错
 *
 * 5. 输出格式容易看错
 *    题目只要求输出：
 *      - 按顺序遇到过的所有对手
 *      - 最终战绩
 *    不需要输出自己名字，也不需要输出每场比赛的赢家
 *
 * 【本题收获】
 *
 * 1. 复杂模拟题要先拆模块，不要一上来整题硬写
 *    这题就很适合拆成：
 *      - Team 结构体
 *      - compute_bu
 *      - play_match
 *      - play_one_round
 *      - 主循环与输出
 *
 * 2. 状态变化题里，尽量用“编号/下标”统一管理对象
 *    比直接复制对象更稳，也更容易避免状态不同步
 *
 * 3. 规则题最怕“差不多对”
 *    特别是这种带伪随机函数的题，顺序只要错一点，答案就全错
 *    所以必须逐条把题意落到代码里
 */
/**
 * 【这道题最值得吸收的营养】
 *
 * 这题的价值很高，而且它不像某些题那样只是学一个套路。
 * 它更像一类“工程化模拟题”或者“小型项目题”：
 *
 * - 不是靠一个神奇算法秒掉
 * - 而是要先设计好结构体
 * - 再维护一堆动态状态
 * - 最后把规则拆成几个稳定模块
 *
 * 这种感觉和很多 C 语言 lab、小型游戏项目很像。
 * 例如一些经典的小作业/小游戏，本质也都是：
 * “定义对象 -> 维护状态 -> 按规则更新 -> 输出结果”。
 *
 * --------------------------------------------------
 * 【为什么这题值得反复吸收】
 *
 * 这题最重要的，不是“过了”本身，而是它训练了：
 *
 * 1. 面对复杂长题面的拆题能力
 * 2. 数据结构设计能力
 * 3. 状态统一管理能力
 * 4. 按严格顺序模拟规则的能力
 *
 * 它更接近“真正写程序”，而不是单纯刷一道模板算法题。
 *
 * --------------------------------------------------
 * 【第一层营养：学会拆题】
 *
 * 这题如果一上来就整题硬写，很容易直接写乱。
 * 正确方法是先拆模块。
 *
 * 最后我们实际上把整题拆成了：
 *
 * - Team 结构体
 * - compute_bu
 * - play_match
 * - play_one_round
 * - 主循环
 * - 查询输出
 *
 * 这是一种非常典型的“工程化拆分”。
 *
 * 以后再遇到复杂模拟题，可以先问自己：
 *
 * - 这题能不能先拆成几个函数？
 * - 哪些是状态对象？
 * - 哪些是单轮逻辑？
 * - 哪些是全局流程？
 *
 * 这比只记住“瑞士轮怎么模拟”更有价值。
 *
 * --------------------------------------------------
 * 【第二层营养：对象状态统一管理】
 *
 * 这题里每支队伍的状态都会不断变化：
 *
 * - win
 * - lose
 * - bu
 * - isleave
 *
 * 一开始如果直接把整个 Team 对象存进历史记录，
 * 后面再算 Bu 时，就会读到“旧拷贝”，而不是当前状态。
 *
 * 所以这题里一个非常重要的经验是：
 *
 * 当对象状态会不断变化时，
 * 最好统一存一份，再通过编号/下标去引用它。
 *
 * 这也是为什么：
 *
 * - opponents 存的是对手下标
 * - 而不是直接存整个 Team 对象
 *
 * 这个经验非常通用。
 * 以后写 lab、写小游戏、写管理系统，都会反复碰到。
 *
 * --------------------------------------------------
 * 【第三层营养：区分“全局动态量”和“阶段固定量”】
 *
 * 这题最容易错的地方之一，就是 Bu 分。
 *
 * 题目规定：
 *
 * - Bu 分在一整轮内固定
 * - 只能在一轮开始时统一计算
 * - 不能打一场变一次
 *
 * 这训练的是一种很重要的时序意识：
 *
 * - 哪些数据是“全局动态变化”的？
 * - 哪些数据是“一轮固定不变”的？
 *
 * 以后很多模拟题、状态机题、游戏更新逻辑都会有这种区别。
 *
 * 这题给你的核心经验是：
 *
 * 在动手写之前，先问自己：
 * “这个量是实时更新的，还是这一阶段固定的？”
 *
 * --------------------------------------------------
 * 【第四层营养：函数职责尽量单一】
 *
 * 这题后面之所以能写顺，
 * 很大程度上是因为函数职责被拆得比较清楚。
 *
 * 例如：
 *
 * - compute_bu 只负责计算 Bu
 * - play_match 只负责打一场比赛并更新状态
 * - play_one_round 只负责完成一整轮流程
 *
 * 这样每个函数都只做一件事，调试和维护就会容易很多。
 *
 * 这也是非常重要的代码设计习惯：
 *
 * 一个函数尽量只承担一种职责，
 * 不要把“排序、配对、比赛、输出”全塞在一个地方。
 *
 * --------------------------------------------------
 * 【第五层营养：规则模拟题最怕顺序错】
 *
 * 这题用 my_rand() 把这个问题体现得特别明显。
 *
 * 因为它不是普通随机，而是依赖全局 seed 的伪随机函数。
 * 所以：
 *
 * - 一场比赛只能调用一次
 * - 必须严格按照比赛真实发生的顺序调用
 * - 如果组顺序错了，后面全错
 * - 如果某场多调用一次，后面也全错
 *
 * 这件事在很多规则模拟题中都成立：
 *
 * 不仅要关心“做了什么”，
 * 还要非常关心“按什么顺序做”。
 *
 * 这也是写小游戏、事件驱动逻辑、状态机时非常重要的意识。
 *
 * --------------------------------------------------
 * 【如何真正吸收这题，而不是只“借过”一次】
 *
 * 这类题最怕两种状态：
 *
 * 1. 过了就结束，不再回头看
 * 2. 因为借助了帮助，就觉得自己等于白做
 *
 * 其实更合理的看法是：
 *
 * 第一次是“搭脚手架”，
 * 第二次回头复盘、整理和重写关键模块，
 * 才是真正把题目变成自己的。
 *
 * 所以如果要继续吸收这题的营养，
 * 最适合做的不是整题完全重写，而是：
 *
 * - 口头复盘模块结构
 * - 自己重写关键函数
 * - 写题后总结
 *
 * --------------------------------------------------
 * 【建议重点复盘的三个函数】
 *
 * 如果之后想自己再消化一遍，
 * 最值得独立重写的是这三个函数：
 *
 * 1. compute_bu
 *    练的是“如何通过历史记录查当前状态”
 *
 * 2. play_match
 *    练的是“如何把一场比赛封装成独立更新过程”
 *
 * 3. play_one_round
 *    练的是“如何把一轮复杂流程拆成固定步骤”
 *
 * 其中 play_one_round 是整题灵魂。
 *
 * --------------------------------------------------
 * 【关于这题的最终理解】
 *
 * 这题最值得记住的一句话是：
 *
 * 这题教会我的，不是某个算法技巧，
 * 而是面对复杂规则时，如何先设计状态，再分模块模拟。
 *
 * 如果以后再遇到类似题目，
 * 最先该想的不是“有什么高级算法”，
 * 而是：
 *
 * - 我要维护什么状态？
 * - 这些状态放在哪个结构体里？
 * - 规则能拆成哪几个模块？
 * - 每个模块的输入输出是什么？
 *
 * 这才是这道题真正的营养所在。
 */