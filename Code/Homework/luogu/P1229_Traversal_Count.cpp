/**
 * @file P1229_Traversal_Count.cpp
 * @brief 洛谷 P1229 遍历问题 - 前后序推导中序可能数
 *
 * 【题目核心】
 * 1. 已知二叉树的前序(Pre)和后序(Post)序列，求中序(In)可能的排列总数。
 * 2. 核心数学规律：中序不唯一的原因在于“单子节点”现象。
 *    - 如果一个节点 A 只有一个子节点 B：
 *      - B 既可以作为 A 的左孩子，也可以作为 A 的右孩子。
 *      - 无论 B 在左还是在右，其 (Pre, Post) 序列完全一致，但 In 序列不同。
 *
 * 【推导结论】
 * 1. 每发现一个“只有一个子节点”的节点，总的可能性就会乘以 2。
 * 2. 如果树中有 k 个这样的节点，总方案数为 2^k。
 *
 * 【模式匹配】
 * 如何在序列中找到这种“单传”关系？
 * - 在前序 s1 中：s1[i] 是父，s1[i+1] 是子。
 * - 在后序 s2 中：如果它是唯一的儿子，它必然紧挨着父节点，即 s2[j] 是子，s2[j+1] 是父。
 * - 判定条件：s1[i] == s2[j+1] && s1[i+1] == s2[j]。
 */

#include <iostream>
#include <string>
#include <cmath>

using namespace std;

int main()
{
    string s1, s2;
    if (!(cin >> s1 >> s2))
        return 0;

    int single_child_count = 0;

    // 遍历前序序列
    for (int i = 0; i < s1.length() - 1; i++)
    {
        // 在后序序列中寻找对应的反转对
        for (int j = 0; j < s2.length() - 1; j++)
        {
            // 模式匹配：前序中的“父子”对应后序中的“子父”
            if (s1[i] == s2[j + 1] && s1[i + 1] == s2[j])
            {
                single_child_count++;
            }
        }
    }

    // 结果为 2 的 k 次方
    // 使用 long long 以应对 2^63 以内的数据，位运算 1LL << k 是计算 2^k 最快的方法
    long long ans = 1LL << single_child_count;

    cout << ans << endl;

    return 0;
}

/**
 * 【专题小结：二叉树遍历】
 *
 * 1. 结构确定性：
 *    - (前序 + 中序) -> 唯一确定一棵树
 *    - (后序 + 中序) -> 唯一确定一棵树
 *    - (前序 + 后序) -> 不一定唯一（当存在单子节点时产生歧义）
 *
 * 2. 算法思维转换：
 *    - 如果题目要求“求出具体的某种遍历”，通常使用递归切分子串的方法。
 *    - 如果题目要求“求方案数”，通常寻找产生歧义的数学模式。
 *
 * 3. 跨越点：
 *    - 从“模拟建树”到“数学抽象”的转变。
 *    - 深刻理解了二叉树遍历中根、左、右子树的相对位置关系。
 */